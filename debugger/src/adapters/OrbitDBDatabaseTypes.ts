enum OrbitDBDatabaseTypes {
  EventStore = 'eventlog',
  FeedStore = 'feed',
  CounterStore = 'counter',
  DocumentStore = 'docstore',
  KeyValueStore = 'keyvalue'
};

export default OrbitDBDatabaseTypes;
